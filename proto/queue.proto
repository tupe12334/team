syntax = "proto3";

package team;

option go_package = "github.com/tupe12334/team/cli/gen;gen";

import "google/protobuf/empty.proto";
import "google/protobuf/timestamp.proto";

service QueueService {
  rpc Enqueue(EnqueueRequest) returns (EnqueueResponse);
  rpc ListQueue(ListQueueRequest) returns (ListQueueResponse);
  rpc UpdateTask(UpdateTaskRequest) returns (UpdateTaskResponse);
  rpc RemoveTask(RemoveTaskRequest) returns (RemoveTaskResponse);
}

message GitHubIssueRef {
  string organization = 1;
  string repository = 2;
  string number = 3;
}

message CentyIssueRef {
  string organization = 1;
  string repository = 2;
  string number = 3;
}

message JiraIssueRef {
  string id = 1;
}

message IssueRef {
  oneof ref {
    GitHubIssueRef github = 1;
    CentyIssueRef centy = 2;
    JiraIssueRef jira = 3;
  }
}

enum TaskStatus {
  QUEUED = 0;
  RUNNING = 1;
  COMPLETED = 2;
  FAILED = 3;
}

message Task {
  string id = 1;
  IssueRef issue_ref = 2;
  string agent = 3;
  TaskStatus status = 4;
  int32 priority = 5;
  google.protobuf.Timestamp created_at = 6;
  google.protobuf.Timestamp updated_at = 7;
}

message EnqueueRequest {
  IssueRef issue_ref = 1;
  optional string agent = 2;
}

message EnqueueResponse {
  oneof result {
    Task task = 1;
    string error = 2;
  }
}

message ListQueueRequest {}

message TaskList {
  repeated Task tasks = 1;
}

message ListQueueResponse {
  oneof result {
    TaskList ok = 1;
    string error = 2;
  }
}

message UpdateTaskRequest {
  string task_id = 1;
  optional string agent = 2;
  optional int32 priority = 3;
}

message UpdateTaskResponse {
  oneof result {
    Task task = 1;
    string error = 2;
  }
}

message RemoveTaskRequest {
  string task_id = 1;
}

message RemoveTaskResponse {
  oneof result {
    google.protobuf.Empty ok = 1;
    string error = 2;
  }
}
